import 'package:KinBech/model/products.dart';

class Cart {
  final Product product;
  final int quantity;

  Cart(this.product, this.quantity);
}

// List<Cart> democarts = [Cart(productslist[1], 3), Cart(productslist[2], 3)];
